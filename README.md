Create your custom cursor from image file and apply it directly without having to open window setting.
clone repo, install dependencies in requirements.txt, run scripts.
or just download the exe file in release.
